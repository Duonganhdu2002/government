import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const primary = Color(0xFF000000); // Black
  static const primaryLight = Color(0xFFE0E0E0); // Light gray
  static const primaryDark = Color(0xFF121212); // Dark gray (almost black)

  // Secondary colors
  static const secondary = Color(0xFF616161); // Gray

  // Functional colors
  static const error = Color(0xFF000000); // Black
  static const warning = Color(0xFF616161); // Gray
  static const success = Color(0xFF424242); // Gray (darker)
  static const info = Color(0xFF757575); // Gray (lighter)

  // Text colors
  static const textPrimary = Color(0xFF000000); // Black
  static const textSecondary = Color(0xFF616161); // Gray
  static const textLight = Color(0xFF9E9E9E); // Light gray

  // Background colors
  static const background = Color(0xFFF5F5F5); // Very light gray
  static const surface = Color(0xFFFFFFFF); // White

  // Border and divider
  static const border = Color(0xFFE0E0E0); // Light gray
  static const divider = Color(0xFFE0E0E0); // Light gray

  // Other colors
  static const darkGrey = Color(0xFF616161); // Gray
  static const lightGrey = Color(0xFFF5F5F5); // Very light gray
}
